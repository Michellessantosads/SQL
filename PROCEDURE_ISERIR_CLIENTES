CREATE PROCEDURE INSERIRCLIENTE
    @IDCLIENTE INT,
    @NOME NVARCHAR(100),
    @SOBRENOME NVARCHAR(100),
    @EMAIL NVARCHAR(255),
	@CPF BIGINT,
	@ENDERECO NVARCHAR(100),
    @DATACADASTRO DATE
AS
BEGIN
    INSERT INTO CLIENTES (ID_CLIENTE, NOME, SOBRENOME, EMAIL, CPF, ENDERECO, DATA_CADASTRO)
    VALUES (@IDCLIENTE, @NOME, @SOBRENOME, @EMAIL, @CPF, @ENDERECO, @DATACADASTRO);
END;
